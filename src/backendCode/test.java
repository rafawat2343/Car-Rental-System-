package backendCode;

import GUI.Cars_Update;

public class test {

	public static void main(String[] args) {
		
		/*System.out.println(LoginInfo.isLoginInfoEmpty());
		LoginInfo getLog1 = LoginInfo.view();
		if (getLog1 != null) {
            System.out.println(getLog1.toString());
        } else {
            System.out.println("LoginInfo is null");
        }
		
		
		LoginInfo log = new LoginInfo("Karim","1323");
		log.updateLoginInfo();
		System.out.println(LoginInfo.isLoginInfoEmpty());
		LoginInfo getLog2 = LoginInfo.view();
		if (getLog2 != null) {
            System.out.println(getLog2.toString());
        } else {
            System.out.println("LoginInfo is null");
        }
		
		LoginInfo log = LoginInfo.view();
		System.out.println(log.toString());*/ 
	}

}
